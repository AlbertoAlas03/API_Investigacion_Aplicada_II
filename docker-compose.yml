version: '3' #version del docker
services:
  app:
    build: .
    ports:
    #rango de puertos donde va a estar expuesto el contenedor
      - "3000:3001" 
    environment: #variables de entorno importantes para el funcionamiento de la API
      - MONGO_URI=${Mongo_uri}
      - SECRET_KEY=${SECRET_KEY}
      - Email_User=${Email_User}
      - Email_Pass=${Email_Pass}
    volumes:
      - .:/app
    command: npm start #al crearse la imagen del contenedor el se inicializa con npm start
    #command: npm run dev #al crearse la imagen del contenedor el se inicializa con npm run dev